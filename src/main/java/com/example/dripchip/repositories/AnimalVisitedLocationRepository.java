package com.example.dripchip.repositories;

import com.example.dripchip.entity.AnimalVisitedLocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnimalVisitedLocationRepository extends JpaRepository<AnimalVisitedLocation,Long> {
}
